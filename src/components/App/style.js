import { createGlobalStyle } from 'styled-components';

export const GlobalStyles = createGlobalStyle`
  body {
		margin: 0 auto;
		padding: 0;
		max-width: 960px;
  }
`;

export const theme = {
  navy: '#090D45',
  purple: '#4629d3',
  blue: '#295cd3',
};
